@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <title>维生特(上海)有限公司</title>
    <link href="~/layui/css/layui.css" rel="stylesheet" />
</head>
<body class="layui-layout-body">
    <div class="layui-layout layui-layout-admin">
        <div class="layui-header">
            <div class="layui-logo">栏目滚动图片</div>
            <ul class="layui-nav layui-layout-left">
                <li class="layui-nav-item"><a href="@Url.Action("Index","Home")">首页</a></li>
                <li class="layui-nav-item"><a href="">黑名单</a></li>
                <li class="layui-nav-item"><a href="">举报投诉</a></li>
                <li class="layui-nav-item"><a href="">招聘管理</a></li>
                <li class="layui-nav-item"><a href="">问卷管理</a></li>
                <li class="layui-nav-item">
                    <a href="javascript:;">内容管理</a>
                    <dl class="layui-nav-child">
                        <dd><a href="@Url.Action("Index","WST_Detial_Pro")">栏目管理</a></dd>
                        <dd><a href="">栏目公告</a></dd>
                        <dd><a href="@Url.Action("Index","WST_Scroll_Picture")">栏目滚动图片</a></dd>
                        <dd><a href="">文章管理</a></dd>
                        <dd><a href="">文章审核</a></dd>
                        <dd><a href="">视频审核</a></dd>
                    </dl>
                </li>
                <li class="layui-nav-item">
                    <a href="javascript:;">其他管理</a>
                    <dl class="layui-nav-child">
                        <dd><a href="">相关链接</a></dd>
                        <dd><a href="">访问统计量</a></dd>
                    </dl>
                </li>
                <li class="layui-nav-item">
                    <a href="javascript:;">系统管理</a>
                    <dl class="layui-nav-child">
                        <dd><a href="">用户管理</a></dd>
                        <dd><a href="">角色管理</a></dd>
                        <dd><a href="">菜单管理</a></dd>
                        <dd><a href="">日志管理</a></dd>
                    </dl>
                </li>
            </ul>
            <ul class="layui-nav layui-layout-right">
                <li class="layui-nav-item">
                    <a href="javascript:;">
                        <img src="http://t.cn/RCzsdCq" class="layui-nav-img">
                        贤心
                    </a>
                    <dl class="layui-nav-child">
                        <dd><a href="">基本资料</a></dd>
                        <dd><a href="">安全设置</a></dd>
                    </dl>
                </li>
                <li class="layui-nav-item"><a href="">退了</a></li>
            </ul>
        </div>

        <div class="layui-body">
            <!--查询-->
            <div class="layui-form" style="margin-left:15px;margin-top:15px">
                <div class="layui-inline">
                    <div class="layui-inline">
                        <select id="iname" name="stitle">
                        </select>
                    </div>
                </div>
                <button class="layui-btn layui-btn-primary" id="Search"><i class="layui-icon">&#xe615;</i>查询</button>
                <button class="layui-btn layui-btn-primary" id="Reset"><i class="layui-icon">&#xe9aa;</i>重置</button>
                <button class="layui-btn layui-btn-primary" id="btnzeng"><i class="layui-icon">&#xe654;</i>新增</button>
            </div>
            <div style="padding: 15px;">
                <table class="layui-hide" lay-filter="test" id="test"></table>
            </div>
        </div>
        <script type="text/html" id="barDemo">
            <a class="layui-btn layui-btn-xs" lay-event="edit">编辑</a>
            <a class="layui-btn layui-btn-danger layui-btn-xs" lay-event="del">删除</a>
        </script>
    </div>
    <script src="~/layui/layui.js"></script>
    <script src="~/js/jquery-3.3.1.js"></script>
    <script>
        //JavaScript代码区域

        layui.use(['laydate', 'laypage', 'layer', 'table', 'carousel', 'upload', 'element', 'slider', 'form'], function () {
            var laydate = layui.laydate //日期
                , laypage = layui.laypage //分页
                , layer = layui.layer //弹层
                , table = layui.table //表格
                , carousel = layui.carousel //轮播
                , upload = layui.upload //上传
                , element = layui.element //元素操作
                , slider = layui.slider //滑块
                , form = layui.form

            //显示
            table.render({
                elem: '#test'
                , url: 'http://localhost:53305/api/WST_Scroll_Picture'
                , page: true
                , limit: 2
                , cols: [[
                    { field: 'sid', width: 80, title: '序号', sort: true }
                    , { field: 'sprograma', width: 120, title: '栏目', sort: true }
                    , { field: 'stitle', width: 180, title: '题目', sort: true }
                    , { field: 'scontent', width: 180, title: '内容', sort: true }
                    , {
                        field: 'sstate', width: 180, title: '状态', sort: true, templet: function (d) {
                            if (d.dstate == 0) {
                                return "显示";
                            }
                            else {
                                return "隐藏";
                            }
                        }
                    }
                    , {
                        field: 'simg', title: '图片', Width: 180, sort: true, templet: function (d) {
                            var str = '';
                            str += '<img src="' + d.simg + '" width="40" height="40" />';
                            return str;
                        }
                    }
                    , { field: 'slink', width: 180, title: '链接地址', sort: true }
                    , { field: 'stime', width: 180, title: '更新地址', sort: true }
                    , { fixed: 'right', width: 165, align: 'center', toolbar: '#barDemo' }
                ]]

            });

            //监听行工具事件
            table.on('tool(test)', function (obj) { //注：tool 是工具条事件名，test 是 table 原始容器的属性 lay-filter="对应的值"
                var data = obj.data //获得当前行数据
                    , layEvent = obj.event; //获得 lay-event 对应的值
                if (layEvent === 'del') {
                    layer.confirm('真的删除行么', function (index) {
                        $.ajax({
                            url: 'http://localhost:53305/api/WST_Scroll_Picture',
                            type: 'delete',
                            data: '"' + data.sid+'"',
                            success: function (result) {
                                if (result > 0) {
                                    alert("删除成功！");
                                    //重新加载
                                    location.reload();

                                }
                                else {
                                    alert("删除失败！");
                                    location.reload();
                                }
                            },
                            dataType: "json",
                            contentType: "application/json"
                        })
                    });
                } else if (layEvent === 'edit') {
                    layer.msg('编辑操作');
                }
            });

            //查询
            $('#Search').click(function () {
                //执行重载
                table.reload('test', {
                    where: { //设定异步数据接口的额外参数，任意设
                        stitle: $('#iname').val(),
                    }
                    , page: {
                        curr: 1 //重新从第 1 页开始
                    }
                }); //只重载数据
            });

            //查询框重置
            $("#Reset").click(function () {
                $("#iname").val("");
                form.render();
            });

            //绑定查询下拉框
            $(function () {
                    $.ajax({
                        url: 'http://localhost:53305/api/WST_Scroll_Picture',
                        type: 'get',
                        success: function (result) {
                            var str = '';
                            str += '<option value="">--请选择--</option>';
                            $(result.data).each(function () {
                                str += '<option value="' + this.stitle + '">' + this.stitle + '</option>';
                            });
                            $('#iname').empty().append(str);
                            //渲染下拉框
                            form.render('select');
                        }
                    })
                });
        });


    </script>
</body>
</html>
